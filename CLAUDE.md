# word-play

When implementing or modifying a game (a `games/gametypes/<game>.py` module and its frontend
renderer), you must read and follow `docs/game-conventions.md` first. It is the checklist for the
backend interface, the Redis/state contract, websocket reconnect/retry behavior, frontend renderer
shape, and tests that every game must adhere to.
